//Grammar4

module Expressions

context-free start-symbols
    Start

context-free syntax

	Start      = Exp
	Exp.Add    = <<Exp> + <Exp>>
	Exp = Term
	Term.Paren = <(<Exp>)>
	Term.Num = Num

lexical syntax

	Num = "0" | "1"  