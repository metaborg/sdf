module pp

imports

	libstratego-gpp
	runtime/refactoring/-
	include/Completions-parenthesize
	src-gen/pp/Completions-pp

rules
	
  pp-Completions-string =
    parenthesize-Completions
    ; prettyprint-SDF-start-symbols
    ; !V([], <id>)
    ; box2text-string(|120)
      
  pp-partial-Completions-string =
    parenthesize-Completions
    ; prettyprint-example
    ; !V([], <id>)
    ; box2text-string(|120)
      
  pp-debug :
    ast -> result
    with
       result := <pp-Completions-string> ast
    <+ <bottomup(try(not(is-string); not(is-list); not(pp-Completions-string); debug(!"cannot pp ")))> ast
    ;  result := ""

rules
  
  /**
  * Given an old AST and a new AST, pretty-prints the new AST while presevering 
  * layout (e.g. whitespacing and comments) associated with the old AST.
  *
  * Example:
  *   new-text := <construct-textual-change> (old-ast, new-ast)
  */
  construct-textual-change = construct-textual-change(pp-partial-Completions-string, parenthesize, override-reconstruction, resugar)

