module generation/to-str

imports
  
  include/TemplateLang
  libstrc
  
rules
  
  to-str-import(|ext):
    imports(i*) -> Imports(<map(!Import(<to-str-module-name(|ext)>))> i*)
    
  to-str-module-name(|ext):
    'module(unparameterized(m)) -> <conc-strings> (m, ext)
    
